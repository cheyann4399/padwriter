<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp">

    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textSize="28sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/permission_guide"
        android:textSize="16sp"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Overlay Permission -->
    <TextView
        android:id="@+id/overlay_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/overlay_permission"
        android:textSize="16sp"
        android:layout_marginTop="32dp"
        app:layout_constraintTop_toBottomOf="@id/subtitle"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/overlay_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/grant_permission"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/overlay_label"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Audio Permission -->
    <TextView
        android:id="@+id/audio_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/audio_permission"
        android:textSize="16sp"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/overlay_button"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/audio_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/grant_permission"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/audio_label"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Accessibility Permission -->
    <TextView
        android:id="@+id/accessibility_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/accessibility_permission"
        android:textSize="16sp"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/audio_button"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/accessibility_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/open_settings"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/accessibility_label"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Start Button -->
    <Button
        android:id="@+id/start_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/start_floating_ball"
        android:layout_marginTop="32dp"
        android:enabled="false"
        app:layout_constraintTop_toBottomOf="@id/accessibility_button"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- WriterPad Button -->
    <Button
        android:id="@+id/writer_pad_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/open_writer_pad"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/start_button"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Settings Button -->
    <Button
        android:id="@+id/settings_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/settings_title"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/writer_pad_button"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
